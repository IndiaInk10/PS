#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int N, M, cards[100];

	scanf("%d %d", &N, &M);

	for (int i = 0; i < N; i++)
	{
		scanf("%d", &cards[i]);
	}

	for (int j = 0; j < 3; j++)
	{

	}

	return 0;
}